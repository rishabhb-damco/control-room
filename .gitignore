# Python virtual environments (rebuild fresh per machine — never portable)
.venv/
venv/
__pycache__/
*.pyc

# Node dependencies (rebuild fresh per machine — never portable)
node_modules/
.next/

# Nested git repos — already independently versioned, never embed as gitlinks
Wellspring/wellspring-ops-app/
Wellspring/wellspring-ops-web/

# OS cruft
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/

# Secrets — never commit
.env
*.env
credentials.json
*.key
*.pem
**/.claude/settings.local.json

# Temp / backup files
~$*
*.tmp
